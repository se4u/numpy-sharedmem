#!/usr/bin/env python
# copied from cython example to run setup using setuptools
"""
script to use setuptools to invoke setup
adds egg features
"""
from setuptools import setup
execfile('setup.py')
